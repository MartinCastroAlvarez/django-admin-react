// @dar/sidebar — the SPA navigation chrome. The app shell renders
// <Sidebar/> beside its own <main>; everything else (drawer state,
// brand, per-user actions, model filter, nav) is self-contained here.

export { Sidebar } from './Sidebar';
